# The actions this canary uses, and the Sabline Action's tag: a new Sabline
# release reaches the Action job as a pull request moving its tag.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: daily
